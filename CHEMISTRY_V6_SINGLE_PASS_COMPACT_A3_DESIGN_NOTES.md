# 化学 V6 A3：单调用紧凑任务结构实验

## 目标

A2 两阶段完整任务图适合定位“题面重建错误、任务关系错误、定档错误”分别发生在哪里，但每题需要两次模型调用，而且完整节点和边的输出很长。591题实跑速度约为每题7秒，不能作为生产候选。

A3 回到单次调用。它不放弃任务结构，而是只保留影响相邻档位判断的最小信息：最高难任务、是否共享题目特有模型，以及最多两条“A判断改变B任务”的决定性依赖。

## 单题执行顺序

1. 同时读取图片与完整结构化文字，检查输入可靠性。
2. 在模型内部恢复全部小问、选项、流程、装置、图表和前后问关系。
3. 选择最高要求任务及不可缺少的前置任务。
4. 内部识别最小任务结构，区分纵向依赖D与整题任务负担B。
5. 按教师五档标准、化学专项协议和真实边界例题定档。
6. 比较相邻低档与相邻高档并冻结档位。
7. 只输出紧凑JSON，不输出完整节点图、边列表或推理草稿。

## 为什么保留紧凑任务结构

- 单纯输出档位无法判断错误来自题面读取、最高任务选择还是边界判断。
- 完整任务图的节点粒度不稳定，也会增加输出token和调用次数。
- `shared_model`用于表达横向共享背景，不虚构有向路径。
- `decisive_dependencies`最多两条，只有前一步的具体输出改变后一步对象、条件、证据、反应集合或模型时才允许填写。
- 这些字段用于逐题审计，不按数量机械映射档位。

## 后处理边界

A3首轮不自动改档。程序只记录三类候选矛盾：低结构高估、拔高但缺少决定性依赖、非特殊锚点压轴但缺少耦合。`postprocess_original_level`、`postprocess_final_level`和`difficulty_level`必须相同，因此raw与final ACC理论上完全一致。

只有窄规则在重复运行和人工逐题复核中证明净收益为正，后续版本才允许最多移动相邻一档。

## 本轮验收

- 591题失败数和Schema/JSON重试数；
- 总耗时、平均API耗时和token；
- 教师原始label下的raw ACC；
- 与V6的逐题得失及档位分布；
- 紧凑任务字段的错误归因能力；
- 相同版本重复运行的档位与结构稳定性。

本轮不能仅凭一次ACC判断架构优劣。如果速度仍明显慢于V6，需要继续压缩输出；如果速度合格但准确率下降，则按逐题差分确认是题面重建、最高任务、依赖抽取还是边界规则造成，而不是继续堆叠feature。
