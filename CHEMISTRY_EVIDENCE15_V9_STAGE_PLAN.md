# Evidence-15 V9 分阶段修改与归因方案

目标是逐阶段验证具体机制，不在同一阶段同时改动 Prompt、示例和自动后处理。

## 阶段1：结构解耦（已完成）

- 真实解题任务先于15项特征。
- 15项特征统一编号并保留严格枚举。
- 取消入口操作与总推理深度、单问内部依赖与跨小问依赖的错误硬绑定。
- 保留严格 Schema、自动修复重试、图片输入和30并发。
- 不改教师边界和既有后处理算法。

阶段1只回答：结构解耦能否提高合法输出并减少错误硬约束。

## 阶段2：相邻边界与真实任务识别（已完成首次验证）

- 修正1↔2：多个直接检索点不能凭任务数量升档；基础题必须存在真实规则应用或实质转换。
- 修正2↔3：除“前序结论被后续使用”外，恢复完整实验、流程、证据和定量任务闭环。
- 修正3↔4：拔高题允许“决定性卡点”或“同一主线高密度决策闭环”两条路径。
- 修正4↔5：允许答案依赖和模型依赖；单问内部、跨阶段、跨区域和跨小问关系均可作为证据。
- 15项事实特征填写后允许回到真实任务做相邻边界终审，但禁止按特征数量投票。

本阶段不改 Schema 字段、不整理全部教师示例、不改自动后处理算法。主要比较阶段1与阶段2的 raw 输出，避免后处理掩盖 Prompt 效果。

首次591题验证显示：纯直接检索识别、完整任务闭环和模型依赖在部分题目上有效，但Prompt内重复定义仍造成双向错档；有效决策数量门槛同时制造拔高低估和压轴高估。因此在教师示例全面整理前，增加阶段2.1做最小一致性修复。

## 阶段2.1：定义一致性修复（已完成验证）

- 1↔2统一为两条基础题路径：由具体条件/现象/数据推出新结论的实际应用，或在易混标准间切换并排除真实干扰项的竞争辨析。
- 2↔3统一为三条中等题路径：连续依赖、完整闭环、综合核验；答案不直接复用不能单独作为降档理由。
- 3↔4统一为两条拔高题路径：决定性卡点，或围绕同一题目模型的综合主线。
- 4↔5改为跨阶段联合模型：前序高阶阶段必须具体改变后续模型、数据段、有效范围、误差处理或计算。
- 删除“5—6个有效决策判拔高、6个以上判压轴”等步骤数量硬门槛。
- 正式定义、边界总结、自检和输出模板只保留同一套判据，不再用近义表述重复定义。

本阶段只改Prompt及版本说明；15项字段、Schema校验、图片输入、重试、30并发和阶段1后处理算法保持不变。评测仍以raw结果为主。

591题验证表明：3↔4的“决定性卡点/综合主线”取得明确收益，教师4档最终正确数增加10题；后处理也从raw净修正9题。主要回退集中在1↔2：教师1档中大量化学变化、材料类别、化学用语、安全规范和低碳行为等教材原型题，被“根据定义逐项判断”错误解释为实际应用。2↔3无净收益，5档召回未提升。

## 阶段2.2：1↔2教材原型边界收窄（已完成验证）

- 保留阶段2.1已经有效的3↔4、4↔5定义，不重复修改中高档机制。
- 1档明确包含熟悉教材原型归类：物理/化学变化、缓慢氧化、物质和材料类别、常见化学式与离子符号、安全规范、低碳行为、延展性及固定图标/仪器识别。
- “根据定义、应用规则、逐项判断”本身不再作为1层或2档证据；同一任务存在两种描述时执行最低充分操作原则。
- 2档真实推导必须满足“题设新增信息→规律→超出类别标签的新结论”；实质竞争辨析必须满足“两个表面合理解释→决定性差异→排除结果”。
- 普通选择题排除错误项、同一标准下逐项分类和规范正误核对不得触发竞争辨析。
- Schema、15项枚举、图片输入、重试、30并发和后处理算法继续保持不变。

所有正式评测的唯一标准标签是教师清洗CSV中的`standard_level`。结果JSONL顶层`difficulty`是旧输入标签且属于错误标签，评测和逐题分析必须明确忽略；评测报告需记录该标签来源与忽略策略。

591题配对验证显示：阶段2.2修正了52道教师1档题，但同时使52道教师2档题回退；准确率只比阶段2.1净增3题，MAE和严重错档变差。失败机制是把“教材原型”扩成1档题型白名单，并用“必须产生隐藏新结论”作为基础题硬门槛。教师2档中存在大量熟悉知识的应用、多个不同判据的独立核验和知识+情境任务，因此该单轴定义与教师口径不一致。

## 阶段2.3：教师口径重新校准（已完成验证）

- 撤销按化学变化、材料类别、化学用语、实验规范等知识点名称建立的1档白名单。
- 1↔2同时检查单项操作D和整题任务负担B：1档是单一固定对应或同一透明规则重复；2档可由一次具体规则应用，或多个不同判据的独立应用束成立。
- 不再要求2档必须产生“超出教材类别标签的隐藏结论”；类别、正误、原因、操作和简单定量结果都可成为一次应用的输出。
- 独立应用束必须列出至少两项不同的实际核验内容，不能以选项、小问或知识词数量机械升档。
- 新增教师1/2档对照锚点：同一标准的化学变化判断为1档；溶液多判据核验、化学方程式不同错误类型核验和操作图规范应用为2档。
- 保留阶段2.1已取得收益的3↔4、4↔5机制；Schema、15项枚举、图片输入、重试、30并发和后处理算法不变。

本阶段仍以raw为主要归因指标，并同时报告final。正式评测只使用教师清洗CSV的`standard_level`，继续忽略结果JSONL顶层旧`difficulty`。

591题验证显示：阶段2.3最终正确359题，比阶段2.2增加17题。39道阶段2.2明确误压的教师2档题中恢复34道，并保留52道教师1档收益中的42道，说明1↔2联合判断D与B的方向有效。剩余主要损失集中在2↔3：教师3档仅正确54题，81题被判为2档。逐题核对发现，模型把“选项之间不复用答案”错误等同于“选项内部只有1层”，并把共享曲线、实验过程、反应后状态和定量对象拆成多个独立应用。

## 阶段3：2↔3判定顺序与教师锚点（当前阶段）

- 不新增题型白名单、步骤数量阈值或特征计数器。
- 先逐选项/逐小问计算内部最长必要过程D，再判断整题是否共享实验状态、流程节点、物质组成、图表阶段或定量对象B。
- “选项独立、无多问、答案不复用”只描述横向关系，不得覆盖某个选项内部的2-3层连续过程。
- 只有所有局部任务均为0-1层，并且不存在共享建模、完整闭环或联合筛选时，才允许用独立一步应用束支持基础题。
- 增加教师中等题正锚点：溶解度曲线多项核验、粗盐提纯完整实验过程；与基础题的操作图一步核验、溶液事实多判据形成对照。
- 保留阶段2.3的1↔2机制和阶段2.1的3↔4、4↔5机制；Schema、15项枚举、图片输入、重试、30并发和后处理算法不变。

本阶段以raw为主要归因指标，同时报告final；正式评测继续只使用教师CSV的`standard_level`。

## 阶段4：后处理离线治理

- 冻结阶段2/3的多次 raw 输出后再开发候选规则。
- 候选规则只观察和记录，不自动改档。
- 按规则统计触发数、改对数、改错数、净收益及跨运行稳定性。
- 只考虑多特征联合、单题最多相邻调整一档的规则。

本阶段不改 Prompt，避免把模型改进与后处理收益混在一起。

## 阶段5：窄规则启用与发布验证

- 只启用在多次独立运行中保持正净收益、误触发可解释的窄规则。
- 全量复跑并同时报告 raw、final、每档召回、严重偏差和规则级收益。
- 若规则只在单次输出有效，则保持观察状态，不进入正式版本。
