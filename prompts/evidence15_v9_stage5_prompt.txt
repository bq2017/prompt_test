from pathlib import Path


_BASE_PATH = Path.cwd() / "prompts" / "evidence15_v9_prompt.txt"
_namespace: dict[str, object] = {}
exec(_BASE_PATH.read_text(encoding="utf-8"), _namespace)
DIFFICULTY_RATING_PROMPT_PREFIX = str(_namespace["DIFFICULTY_RATING_PROMPT_PREFIX"])


def _replace_once(old: str, new: str) -> None:
    global DIFFICULTY_RATING_PROMPT_PREFIX
    count = DIFFICULTY_RATING_PROMPT_PREFIX.count(old)
    if count != 1:
        raise RuntimeError(f"Stage5替换锚点数量异常: expected=1 actual={count}: {old[:60]!r}")
    DIFFICULTY_RATING_PROMPT_PREFIX = DIFFICULTY_RATING_PROMPT_PREFIX.replace(old, new, 1)


_replace_once(
    "- 6层及以上：至少两个高阶处理阶段通过物质、条件、参数、数据段或中间结论耦合，前一阶段的结果会改变后一阶段所用模型、有效范围或最终计算。",
    "- 6层及以上：至少两个实质高阶任务通过模型选择、共同闭合或同一化学状态的连续更新形成不可分离关系。这里的“6层及以上”只是结构审计枚举，不按决策数、步骤数或小问数触发。",
)

_replace_once(
    """#### 6. 拔高题与压轴题：单一高阶主线和跨阶段联合模型必须分开

拔高题有两条通道：一是存在一个会改变后续路径的决定性卡点；二是没有单一卡点，但同一题目模型同时承载组成/状态建立、证据筛选、方程关联、图表或数据处理、实验评价、参数约束等不同类型任务，形成一条实质综合主线。综合主线不能由无关小问或知识点名称机械相加。

压轴题不以步骤数量为门槛。必须至少包含两个高阶处理阶段，并证明前一阶段确定的物质组成、实验条件、参数、数据范围或中间结论会改变后一阶段的反应模型、数据选择、有效解范围或最终计算。答案依赖和模型依赖均可作为证据。

压轴终审时，必须用自然语言写出具体的阶段关系，例如：

- 成分判断 → 反应方程式选择 → 定量模型；
- 装置限制 → 证据是否有效 → 方案修改；
- 图像阶段 → 反应状态 → 守恒或联立；
- 前一实验结论 → 后续试剂选择 → 误差或纯度计算。

若题目是单一设问，也可写设问内部的阶段关系，不要求人为拆成小问。若多个模块只是并列出现、后续模型不受前序结论影响，则保持拔高题比较。反之，只要多个高阶阶段真实改变后续模型，即使每个局部步骤都常规，也可以进入压轴题比较。

模型依赖必须同时写清：共享的题目特定对象或参数；分别需要建立的阶段或状态；前序结果具体改变了后续哪一个方程、数据段、反应模型、有效范围或误差处理。仅“多问、流程长、知识多、装置复杂”不满足。""",
    """#### 6. 拔高题与压轴题：用不可分离强关系统一终审

拔高题有两条成立路径：一个明确且会改变解法的决定性卡点；或不同实质任务围绕同一题目模型形成一条综合主线。综合主线不能由无关小问、知识点名称或题面长度机械相加。

压轴题只保留一个核心标准：最高难任务中至少存在两个实质高阶任务H1、H2，并且二者通过以下一种强关系形成不可分离结构：

- 模型选择：H1决定H2所用的反应对象、反应路径、方程、数据段、候选范围或状态分类；
- 共同闭合：H1与H2共同确定同一个未知状态或未知量，任一任务单独都不能闭合结论；
- 状态更新：多个阶段连续更新同一化学体系的组成或状态，使H2面对的对象、路径或约束随H1结果改变。

“实质高阶任务”必须自身包含会改变解法的化学判断，例如反应路径或状态分类、竞争证据排除、图像阶段反推、未知组成建模、多关系定量闭合、实验方案或证据有效性评价。抄写方程式、直接比例代入、纯算术、重复核验和机械操作不算独立高阶任务。

判5前必须执行“已知结果替代测试”：假定H1的结果已经直接给出，H2能否仍按原来的反应对象、方程、数据范围和模型独立完成？

- 若能，只是普通结果传递或数值代入，不构成5档强关系；
- 若不能，且原因是H1改变了H2的模型、状态、范围，或H1与H2必须共同闭合同一未知量，才进入5档比较。

多个模块并列、流程顺序长、图表实验计算同时出现、前一步数字供后一步代入，都不能替代上述测试。题目可以是单一设问，也可以跨阶段或跨小问；只核对真实关系，不人为拆分或计数。

若5档标准不成立，只能得出“不判5档”，不能直接降为3档。必须独立检查是否存在4档的决定性卡点或综合主线；只有4档两条路径也都不成立时，才与3档比较。

若判5档，`core_basis`必须按以下内容写清：`高阶任务H1：……；高阶任务H2：……；关系类型：模型选择/共同闭合/状态更新；已知结果替代测试：……`。若关系类型只能写“无强关系”，不得判5档。""",
)

_replace_once(
    "6. 若最终判压轴题，是否写清至少两个高阶处理阶段，以及前序结果具体改变了后续哪个模型、数据段、有效范围或计算。",
    "6. 若最终判压轴题，是否写清高阶任务H1、H2、三类强关系之一及已知结果替代测试；普通数值代入是否已被排除。",
)

_replace_once(
    "两条路径都仍可沿一条有限主线解决，尚未形成跨高阶阶段的联合模型。",
    "两条路径都仍可沿一条有限主线解决，尚未形成两个实质高阶任务之间的模型选择、共同闭合或状态更新关系。未通过5档测试后仍须独立保留本档判断，不能直接降到中等题。",
)

_replace_once(
    "既无法说清具体区分度卡点，也无法写清不同实质任务如何围绕同一题目模型共同限制结论时，不得判拔高题；若前一高阶阶段已经改变后一阶段的模型、数据段、有效范围或计算，则不得停在拔高题。",
    "既无法说清具体区分度卡点，也无法写清不同实质任务如何围绕同一题目模型共同限制结论时，不得判拔高题；若H1、H2通过模型选择、共同闭合或状态更新形成不可分离强关系，且通过已知结果替代测试，则不得停在拔高题。",
)

_replace_once(
    "压轴题要求形成跨阶段联合模型：至少两个高阶处理阶段通过物质、条件、参数、证据、数据段或中间结论发生实质关系，前一阶段的结果会改变后一阶段所用反应模型、数据选择、有效解范围、误差处理或最终计算。它可以表现为多模块深度融合、完全陌生模型现场建立、多层嵌套约束、多重守恒/差量/联立、多阶段实验与定量误差，或反应—证据—条件—图像—定量的跨阶段联合。",
    "压轴题要求至少两个实质高阶任务H1、H2形成不可分离强关系：H1决定H2的模型选择，H1与H2共同闭合同一未知状态或未知量，或多个阶段连续更新同一化学体系的组成与状态。题型名称、特征数量和步骤数量都不能替代这一结构判断。",
)

_replace_once(
    "- 可以不同时命中全部典型特征；决定性依据是前序高阶阶段是否实质改变后续模型，而不是步骤数量或“红线计数”。",
    "- 可以不同时命中全部典型特征；决定性依据是H1、H2是否形成模型选择、共同闭合或状态更新关系，并通过已知结果替代测试，而不是步骤数量或“红线计数”。",
)

_replace_once(
    "必须写清至少两个高阶处理阶段，并说明前一阶段得到的组成、条件、参数、数据范围或中间结论具体改变了后一阶段的哪个方程、反应模型、数据段、有效解范围、误差处理或最终计算。若后续阶段不受前序结果影响，只是顺序排列，则不能触发压轴题。只列举高阶特征名称不算触发。",
    "必须写清高阶任务H1、H2及其关系类型，并执行已知结果替代测试。若给定H1结果后，H2仍能按原对象、方程、数据范围和模型独立完成，则只是普通结果或数值传递，不能触发压轴题。只列举高阶特征名称不算触发。",
)

_replace_once(
    "不能指出两个高阶处理阶段以及前序结果对后续模型的具体影响时，不得判压轴题；已确认多层嵌套、现场建模或多重定量跨阶段联合时，不得因每个局部步骤看似常规而压回拔高题。",
    "不能指出两个实质高阶任务，或二者不属于模型选择、共同闭合、状态更新中的任何一种，或无法通过已知结果替代测试时，不得判压轴题；但必须重新独立检查拔高题的决定性卡点与综合主线，不能因5档否决直接压到中等题。",
)

_replace_once(
    "边界总结：拔高题是单一高阶卡点或一条综合主线；压轴题要求至少两个高阶处理阶段，并且前序结果会具体改变后续的反应模型、数据段、有效范围、误差处理或最终计算。多个模块并列、流程顺序长或步骤数量多都不能单独触发压轴题。",
    "边界总结：拔高题由单一高阶卡点或一条综合主线成立；压轴题要求两个实质高阶任务形成模型选择、共同闭合或状态更新关系，并通过已知结果替代测试。普通数值传递、模块并列、流程长或步骤多都不能单独触发压轴题；5档不成立后仍须独立复核4档。",
)

_replace_once(
    "11. 若判5档，能否指出至少两个高阶处理阶段，并写清前序结果具体改变了后续哪个模型、数据段、有效范围、误差处理或计算？",
    "11. 若判5档，能否写清高阶任务H1、H2、关系类型和已知结果替代测试，并排除普通数值传递？",
)

_replace_once(
    '"core_basis": "用2-4句话说明最高难任务属于单一固定对应、一次具体规则应用、多个不同判据的独立应用束、常规综合、单一高阶卡点、综合主线或跨阶段联合模型，并引用真实核心特征",',
    '"core_basis": "用2-4句话说明最高难任务属于单一固定对应、一次具体规则应用、多个不同判据的独立应用束、常规综合、单一高阶卡点或综合主线，并引用真实核心特征；若判压轴题，必须另写高阶任务H1、H2、关系类型和已知结果替代测试",',
)
