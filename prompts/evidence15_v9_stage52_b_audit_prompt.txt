DIFFICULTY_RATING_PROMPT_PREFIX = r"""
# Stage5.2-B：初中化学复合负担独立审计

你只判断题目是否满足“严格复合负担B路径”。输入题目已由另一个冻结模型
筛选为原始4档候选，但你看不到它的判档理由、特征、教师标签或教师理由。
不得重新评价1～4档，不得输出最终难度档位。

## B成立的四个必要条件

### 1. 同一最高难任务

先锁定决定整题最高难度的同一个任务。X、Y必须共同服务于该任务的同一
最终目标。两个独立小问、不同选项、实验部分与计算部分横向累加，或仅共享
工业/实验/材料背景，都不满足本条件。

### 2. 现场构造中间模型M

题面没有直接提供可完整套用的模型，解题者必须自行构造状态表示、候选空间、
关系模型、约束系统或分段反应框架中的至少一种。

直接套用教材中的单一方程、普通比例、固定守恒式、标准图像读数，只把条件
换成符号，或仅因物质名称和背景陌生，均不算现场构造。

### 3. 两个不可约的高阶因果功能X、Y

X、Y必须承担不同因果功能，例如生成候选、排除候选、确定状态、选择模型、
限定范围、建立独立约束、验证证据或完成定量闭合。

实验、图表、计算、流程只是表面信息形式，不算不同因果功能。不得把同一个
判断人为拆成两步；直接识记、透明映射、普通数值传递、纯算术、抄写方程和
机械执行不算高阶因果功能。

### 4. 不可拆分且不是机械长尾

必须同时满足：

- X、Y不能分别求解后直接拼接成完整答案；
- X、Y不能压缩为同一个判断；
- X或Y都不是建立M之后的机械执行；
- 删除X或Y后，共同目标会出现多个可行解释、证据不足或无法闭合；
- 整体不能退化为“一个高阶卡点加常规推进”。

删除一问后仅仅少一个独立答案，不算闭合破坏。普通数值传递连接两个模块，
不能作为B成立证据。

## 禁止作为B证据

综合性强、篇幅较大、过程较多、知识点多、小问多、同时含实验/图表/计算、
工业流程复杂、多步计算、陌生材料、多个小问都较难，均不能单独或组合支持B。

## 输出

只输出一个完整JSON对象，不要输出Markdown。必须且只能包含以下字段：

{
  "constructed_representation": "现场构造的中间模型M；不存在则说明最先失败原因",
  "representation_nonroutine": true,
  "operation_x": "不可约高阶因果功能X；不存在则说明原因",
  "operation_y": "不同因果功能Y；不存在则说明原因",
  "different_causal_roles": true,
  "same_hardest_task": true,
  "independently_solvable": false,
  "answer_splicing": false,
  "mechanical_tail": false,
  "delete_x_effect": "closure_breaks/only_loses_independent_answer/no_material_change",
  "delete_y_effect": "closure_breaks/only_loses_independent_answer/no_material_change",
  "reason": "逐项说明四个必要条件是否满足；不得使用题长、综合或步骤多作理由"
}

布尔字段必须使用JSON的true或false。不要自行输出composite_burden或最终档位；
外部程序会根据上述有限字段确定性计算B是否成立。
"""

DIFFICULTY_RATING_PROMPT_SUFFIX = r"""

请独立阅读题目、图片、标准答案和必要解析，输出严格B审计JSON。
"""
